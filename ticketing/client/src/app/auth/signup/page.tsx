'use client';
import { FC } from 'react';
import { ProfileForm } from './components/ProfileForm';

interface PageProps {}

const Page: FC<PageProps> = ({}) => {
  return <ProfileForm />;
};

export default Page;
